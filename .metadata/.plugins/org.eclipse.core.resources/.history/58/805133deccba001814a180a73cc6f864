package br.senac.rn.loja.dao;

import java.util.ArrayList;
import java.util.List;

import br.senac.rn.loja.model.Sexo;

public class SexoDAO {
	
	private static List<Sexo> sexos = new ArrayList<>();
	
	public void inserir(Sexo sexo) {
		sexos.add(sexo);
	}

	public void remover(Sexo sexo) {
		sexos.remove(sexo);
	}
	
	public void editar(Sexo sexo) {
		Sexo s = bucarPorId(sexo.getId());
		if (s != null) {
			s = sexo;
		}
	   	
	}
	
	public List<Sexo> buscarTodos() {
		return sexos;
	}
	
	public Sexo bucarPorId(Integer id) {
		for (Sexo sexo : sexos) {
			if (id == sexo.getId()) {
				return sexo;
			}
		}
		return null;
	}
	
	public void bucarNome() {
		
	}
	
	public void buscarSigla() {
		
	}
}
